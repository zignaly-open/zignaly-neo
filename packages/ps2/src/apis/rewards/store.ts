import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

export const rewardApiSlice = createSlice({
  name: 'rewardApi',
  initialState,
  reducers: {},
});

export default rewardApiSlice.reducer;
