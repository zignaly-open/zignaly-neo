export type InfoBarItem = {
  title: string;
  description: string;
};

export type FeatureItem = {
  title: string;
  description: string;
};

export type HowWorksItem = {
  title: string;
  description: string;
};
