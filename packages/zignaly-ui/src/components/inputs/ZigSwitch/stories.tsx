import { Meta, StoryObj } from "@storybook/react";
import ZigSwitch from "./index";

const meta = {
  title: "Inputs/ZigSwitch",
  component: ZigSwitch,
} as Meta;
export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {};
