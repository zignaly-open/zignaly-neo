import { Meta, StoryObj } from "@storybook/react";
import ZigCheckBox from "./index";

const meta = {
  title: "Inputs/ZigCheckBox",
  component: ZigCheckBox,
} as Meta;
export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {};
