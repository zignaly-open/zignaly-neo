import styled from '@emotion/styled';

export const Table = styled('table')`
  margin: 0 auto;

  td:first-of-type {
    text-align: right;
    padding-right: 8px;
    min-width: 114px;
  }

  td:last-of-type {
    text-align: left;
    min-width: 127px;
  }
`;
