var _a;
import { createSlice } from '@reduxjs/toolkit';
var initialState = {};
export var walletSlice = createSlice({
    name: 'wallet',
    initialState: initialState,
    reducers: {},
});
export var _b = _a = walletSlice.actions;
export default walletSlice.reducer;
//# sourceMappingURL=store.js.map