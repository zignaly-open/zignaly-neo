import { createSlice } from '@reduxjs/toolkit';
var initialState = {};
export var serviceApiKeySlice = createSlice({
    name: 'serviceApiKey',
    initialState: initialState,
    reducers: {},
});
export default serviceApiKeySlice.reducer;
//# sourceMappingURL=store.js.map