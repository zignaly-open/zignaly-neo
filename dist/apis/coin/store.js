var _a;
import { createSlice } from '@reduxjs/toolkit';
var initialState = {};
export var coinsSlice = createSlice({
    name: 'coins',
    initialState: initialState,
    reducers: {},
});
export var _b = _a = coinsSlice.actions;
export default coinsSlice.reducer;
//# sourceMappingURL=store.js.map