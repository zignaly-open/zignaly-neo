name: PS2 Staging deploy
on:
  push:
    branches:
      - master

jobs:
  deploy:
    name: PS2 Staging WL deploy
    uses: ./.github/workflows/ps2-deploy.yml
    secrets: inherit
    with:
      deploy-env: staging-wl
