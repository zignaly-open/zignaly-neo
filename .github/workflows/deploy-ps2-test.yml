name: PS2 Test deploy
on:
  push:
    branches:
      - test
      - fix/no-pwa

jobs:
  deploy:
    name: PS2 Test deploy
    uses: ./.github/workflows/ps2-deploy.yml
    secrets: inherit
    with:
      deploy-env: test
