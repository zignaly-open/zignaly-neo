{
  "name": "zignaly-zigraffle",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "postinstall": "husky install",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "frontend": "cd packages/web && env-cmd -f .env yarn start",
    "backend": "cd packages/server && yarn dev",
    "build": "cd packages/web && yarn && env-cmd -f .env.production GENERATE_SOURCEMAP=false yarn build && cd ../server && yarn",
    "start": "cd packages/server && yarn start"
  },
  "engines": {
    "node": "^16"
  },
  "author": "Alexander Mikhalchenko <alex@xfuturum.com>",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.0",
    "prettier": "^2.6.2",
    "typescript": "4.7.2"
  },
  "dependencies": {
    "env-cmd": "^10.1.0"
  }
}
