{
  "name": "@zignaly/mono",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "postinstall": "husky install",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "frontend": "cd packages/raffles-client && env-cmd -f .env yarn start",
    "backend": "cd packages/raffles-server && yarn dev",
    "build": "cd packages/raffles-client && yarn && env-cmd -f .env.production GENERATE_SOURCEMAP=false yarn build && cd ../server && yarn",
    "start": "cd packages/raffles-server && yarn start"
  },
  "engines": {
    "node": "^16"
  },
  "author": "Alexander Mikhalchenko <alex@xfuturum.com>",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.1",
    "lerna": "^5.1.2",
    "lint-staged": "^13.0.0",
    "typescript": "4.7.2"
  },
  "dependencies": {
    "env-cmd": "^10.1.0"
  }
}
